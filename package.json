{
  "name": "@montacasa/cz-mc",
  "version": "0.2.0-rc.2",
  "description": "Commitizen adapter following the montacasa's changelog format.",
  "main": "index.js",
  "scripts": {
    "commit": "git-cz",
    "test": "echo 'Tests need to be setup!'",
    "semantic-release": "semantic-release pre && npm publish && semantic-release post"
  },
  "homepage": "https://github.com/commitizen/cz-conventional-changelog",
  "license": "MIT",
  "dependencies": {
    "conventional-commit-types": "^2.0.0",
    "lodash.map": "^4.5.1",
    "longest": "^1.0.1",
    "pad-right": "^0.2.2",
    "right-pad": "^1.0.1",
    "word-wrap": "^1.0.3"
  },
  "devDependencies": {
    "@montacasa/cz-mc": "^0.2.0-rc.1",
    "commitizen": "2.9.6"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/@montacasa/cz-mc"
    }
  }
}
